
_ Create 4 files: Style.css index.html app.js(main) connect4.js(class)
_ the app.js will be the main js that calls the connect4 class
_ inside the connect4.js create a class with constructor that have Rows and colums as attributes 
_ create a method (creatgrid) inside the class connect 4 and Use Jquery and loops to create the rows
 and colums on the html web page
_ create eventlistner method to :
1. show the index when we hover over a cell
2. have the ability to clic on a cell
3. alternate colors (to alternate between the two players)
4. add a style for the cursor to show the pointer 
_ add a function that check if there is a winner 